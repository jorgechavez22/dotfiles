# Habilitar características experimentales de Nix
experimental-features = nix-command flakes

# Opcional: Configuraciones adicionales recomendadas
# Permitir builds no libres (si necesitas software propietario)
# extra-experimental-features = ca-derivations

# Cachear evaluaciones para mejor rendimiento
eval-cache = true
